language: java
sudo: false
jdk: 
      - oraclejdk8

before_install:
#  - curl -sL https://github.com/jpm4j/jpm4j.installers/raw/master/dist/biz.aQute.jpm.run.jar >jpm4j.jar
#  - java -jar jpm4j.jar -u init
#  - ~/jpm/bin/jpm install com.codacy:codacy-coverage-reporter:assembly

script:
  - mvn clean install

after_success:
  - mvn com.gavinmogan:codacy-maven-plugin:coverage -DcoverageReportFile=integration-test/target/site/jacoco/jacoco.xml -DprojectToken=$CODACY_PROJECT_TOKEN  -DapiToken=$CODACY_API_TOKEN
#  - ~/jpm/bin/codacy-coverage-reporter -l Java -r integration-test/target/site/jacoco/jacoco.xml --debug
