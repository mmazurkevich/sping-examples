<web-app>
  <display-name>Archetype Created Web Application</display-name>

  <!--The job of the DispatcherServlet is to take an incoming URI and find the -->
  <!--right combination of handlers (generally methods on Controller classes) -->
  <!--and views (generally JSPs) that combine to form the page or resource that's -->
  <!--supposed to be found at that location.-->

  <servlet>
    <servlet-name>mvc-dispatcher</servlet-name>
    <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
    <load-on-startup>1</load-on-startup>
  </servlet>

  <!--Upon initialization of a DispatcherServlet, Spring MVC looks for a file named [servlet-name]-servlet.xml -->
  <!--in the WEB-INF directory of your web application and creates the beans defined there...-->
  <!--That's why created dispatcher-servlet.xml-->

  <!--IF you need custome file name  and application configuration:-->
  <!--  <context-param>
    <param-name>contextConfigLocation</param-name>
    <param-value>/WEB-INF/mvc-dispatcher-servlet.xml</param-value>
  </context-param>-->
  <servlet-mapping>
    <servlet-name>dispatcher</servlet-name>
    <url-pattern>/</url-pattern>
  </servlet-mapping>
</web-app>
